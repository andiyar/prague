name: Keep Supabase Alive

# Runs every 5 days to prevent Supabase free-tier from pausing due to inactivity
on:
  schedule:
    - cron: '0 8 */5 * *'  # Every 5 days at 08:00 UTC
  workflow_dispatch: # Allow manual trigger

jobs:
  ping-database:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase database
        run: |
          response=$(curl -s -o /dev/null -w "%{http_code}" \
            "https://dyxupzbyssvcxjppipnl.supabase.co/rest/v1/config?select=key&limit=1" \
            -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImR5eHVwemJ5c3N2Y3hqcHBpcG5sIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njg5Mjc0MTksImV4cCI6MjA4NDUwMzQxOX0._pmFY2kmyUYLauX-BQeELbWziJ4nuXIaxOM5YsUYsBI" \
            -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImR5eHVwemJ5c3N2Y3hqcHBpcG5sIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njg5Mjc0MTksImV4cCI6MjA4NDUwMzQxOX0._pmFY2kmyUYLauX-BQeELbWziJ4nuXIaxOM5YsUYsBI")

          if [ "$response" -eq 200 ]; then
            echo "Database ping successful (HTTP $response)"
          else
            echo "Database ping failed (HTTP $response)"
            exit 1
          fi
